# GoogleProtobufNullValue

`NullValue` is a singleton enumeration to represent the null value for the
 `Value` type union.

 The JSON representation for `NullValue` is JSON `null`.

## Example Usage

```python
from mistralai.client.models import GoogleProtobufNullValue
value: GoogleProtobufNullValue = "NULL_VALUE"
```


## Values

- `"NULL_VALUE"`
