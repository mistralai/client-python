# WorkerStatus

Filter by worker activity. active=only active, inactive=only inactive, None=no filter

## Example Usage

```python
from mistralai.client.models import WorkerStatus
value: WorkerStatus = "active"
```


## Values

- `"active"`
- `"inactive"`
