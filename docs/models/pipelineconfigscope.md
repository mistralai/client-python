# PipelineConfigScope

## Example Usage

```python
from mistralai.client.models import PipelineConfigScope

# Open enum: unrecognized values are captured as UnrecognizedStr
value: PipelineConfigScope = "workspace"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"workspace"`
- `"shared"`
