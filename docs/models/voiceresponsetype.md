# VoiceResponseType

## Example Usage

```python
from mistralai.client.models import VoiceResponseType

# Open enum: unrecognized values are captured as UnrecognizedStr
value: VoiceResponseType = "preset"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"preset"`
- `"custom"`
