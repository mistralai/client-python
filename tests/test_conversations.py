"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from mistralai.client import Mistral, models
from mistralai.client.utils import parse_datetime
import os
from tests.test_client import create_test_http_client


def test_conversations_agents_api_v1_conversations_start():
    test_http_client = create_test_http_client("agents_api_v1_conversations_start")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.start(
            inputs="<value>",
            stream=False,
            completion_args={
                "response_format": {
                    "type": "text",
                },
            },
        )
        assert res is not None
        assert res == models.ConversationResponse(
            conversation_id="<id>",
            outputs=[
                models.AgentHandoffEntry(
                    previous_agent_id="<id>",
                    previous_agent_name="<value>",
                    next_agent_id="<id>",
                    next_agent_name="<value>",
                ),
            ],
            usage=models.ConversationUsageInfo(),
        )


def test_conversations_agents_api_v1_conversations_list():
    test_http_client = create_test_http_client("agents_api_v1_conversations_list")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.list(page=0, page_size=100)
        assert res is not None
        assert res == [
            models.AgentConversation(
                id="<id>",
                created_at=parse_datetime("2025-11-20T22:30:47.754Z"),
                updated_at=parse_datetime("2025-08-05T08:36:20.296Z"),
                agent_id="<id>",
            ),
        ]


def test_conversations_agents_api_v1_conversations_get():
    test_http_client = create_test_http_client("agents_api_v1_conversations_get")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.get(conversation_id="<id>")
        assert res is not None
        assert res == models.AgentConversation(
            id="<id>",
            created_at=parse_datetime("2023-06-02T14:00:42.201Z"),
            updated_at=parse_datetime("2024-10-06T17:16:50.325Z"),
            agent_id="<id>",
        )


def test_conversations_agents_api_v1_conversations_append():
    test_http_client = create_test_http_client("agents_api_v1_conversations_append")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.append(
            conversation_id="<id>",
            stream=False,
            store=True,
            handoff_execution="server",
            completion_args={
                "response_format": {
                    "type": "text",
                },
            },
        )
        assert res is not None
        assert res == models.ConversationResponse(
            conversation_id="<id>",
            outputs=[],
            usage=models.ConversationUsageInfo(),
        )


def test_conversations_agents_api_v1_conversations_history():
    test_http_client = create_test_http_client("agents_api_v1_conversations_history")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.get_history(conversation_id="<id>")
        assert res is not None
        assert res == models.ConversationHistory(
            conversation_id="<id>",
            entries=[
                models.ToolExecutionEntry(
                    name="image_generation",
                    arguments="<value>",
                ),
            ],
        )


def test_conversations_agents_api_v1_conversations_messages():
    test_http_client = create_test_http_client("agents_api_v1_conversations_messages")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.get_messages(conversation_id="<id>")
        assert res is not None
        assert res == models.ConversationMessages(
            conversation_id="<id>",
            messages=[],
        )


def test_conversations_agents_api_v1_conversations_restart():
    test_http_client = create_test_http_client("agents_api_v1_conversations_restart")

    with Mistral(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_key=os.getenv("MISTRAL_API_KEY", "value"),
    ) as mistral:
        assert mistral is not None

        res = mistral.beta.conversations.restart(
            conversation_id="<id>",
            from_entry_id="<id>",
            stream=False,
            store=True,
            handoff_execution="server",
            completion_args={
                "response_format": {
                    "type": "text",
                },
            },
        )
        assert res is not None
        assert res == models.ConversationResponse(
            conversation_id="<id>",
            outputs=[],
            usage=models.ConversationUsageInfo(),
        )
