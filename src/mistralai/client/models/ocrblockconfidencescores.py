"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: 27e42b07192d

from __future__ import annotations
from mistralai.client.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class OCRBlockConfidenceScoresTypedDict(TypedDict):
    r"""Per-block confidence scores, computed per-word from model logprobs.

    All fields ``None`` when the block couldn't be scored.
    Individual fields ``None`` when that signal is absent — e.g. an image-only block has
    no caption, so content scores are ``None``.
    """

    average_content_confidence_score: NotRequired[Nullable[float]]
    r"""Average confidence over the block's content (caption) tokens. None when the block has no textual content (e.g. image-only entry)."""
    minimum_content_confidence_score: NotRequired[Nullable[float]]
    r"""Minimum per-word content confidence in the block. None when the block has no textual content."""
    block_type_confidence_score: NotRequired[Nullable[float]]
    r"""Confidence in the block type (e.g. 'text', 'title', 'table'). None when the entry had no block type or the block type span could not be located."""


class OCRBlockConfidenceScores(BaseModel):
    r"""Per-block confidence scores, computed per-word from model logprobs.

    All fields ``None`` when the block couldn't be scored.
    Individual fields ``None`` when that signal is absent — e.g. an image-only block has
    no caption, so content scores are ``None``.
    """

    average_content_confidence_score: OptionalNullable[float] = UNSET
    r"""Average confidence over the block's content (caption) tokens. None when the block has no textual content (e.g. image-only entry)."""

    minimum_content_confidence_score: OptionalNullable[float] = UNSET
    r"""Minimum per-word content confidence in the block. None when the block has no textual content."""

    block_type_confidence_score: OptionalNullable[float] = UNSET
    r"""Confidence in the block type (e.g. 'text', 'title', 'table'). None when the entry had no block type or the block type span could not be located."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "average_content_confidence_score",
                "minimum_content_confidence_score",
                "block_type_confidence_score",
            ]
        )
        nullable_fields = set(
            [
                "average_content_confidence_score",
                "minimum_content_confidence_score",
                "block_type_confidence_score",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
