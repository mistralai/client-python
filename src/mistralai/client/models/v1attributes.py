"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: aa0af0e4f6b8

from __future__ import annotations
from .v1extraattribute import V1ExtraAttribute, V1ExtraAttributeTypedDict
from mistralai.client.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class V1AttributesTypedDict(TypedDict):
    title: NotRequired[str]
    description: NotRequired[str]
    r"""Marked `optional` for explicit presence: on update, an absent description
    leaves the stored value unchanged, while a provided value (including the
    empty string) is applied, allowing the description to be cleared.
    """
    extra: NotRequired[Dict[str, V1ExtraAttributeTypedDict]]
    r"""Typed extra metadata. Keys starting with `_` are private and only
    visible/mutable to internal services (presence of `x-internal-service`).
    Supported value types are currently string and repeated string. Number,
    boolean, object, and null values are rejected by the service.
    The service enforces app-level guardrails on the final stored map:
    by default, at most 20 total keys, keys up to 50 characters, string
    values up to 50 characters, and repeated values up to 20 items.
    These runtime limits are configurable via MAX_EXTRA_ATTRIBUTES_*.
    On update, a provided extra map replaces existing extras in the caller's
    writable namespace. Omit a key from the replacement map to delete it.
    """


class V1Attributes(BaseModel):
    title: Optional[str] = None

    description: Optional[str] = None
    r"""Marked `optional` for explicit presence: on update, an absent description
    leaves the stored value unchanged, while a provided value (including the
    empty string) is applied, allowing the description to be cleared.
    """

    extra: Optional[Dict[str, V1ExtraAttribute]] = None
    r"""Typed extra metadata. Keys starting with `_` are private and only
    visible/mutable to internal services (presence of `x-internal-service`).
    Supported value types are currently string and repeated string. Number,
    boolean, object, and null values are rejected by the service.
    The service enforces app-level guardrails on the final stored map:
    by default, at most 20 total keys, keys up to 50 characters, string
    values up to 50 characters, and repeated values up to 20 items.
    These runtime limits are configurable via MAX_EXTRA_ATTRIBUTES_*.
    On update, a provided extra map replaces existing extras in the caller's
    writable namespace. Omit a key from the replacement map to delete it.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["title", "description", "extra"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
